from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project implements mask-RCNN with pretrained weights from the MS COCO dataset on satellite imagery tof humanitarian crises to quantify extent of damage.',
    author='Damien Gulliver',
    license='',
)
